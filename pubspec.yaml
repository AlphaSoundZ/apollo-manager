name: apollo_manager
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
    sdk: '>=3.0.6 <4.0.0'

dependencies:
    flutter:
        sdk: flutter
    dio: ^5.3.1
    encrypted_shared_preferences: ^3.0.1
    flutter_dotenv: ^5.1.0
    get: ^4.6.5
    jwt_decode: ^0.3.1
    pretty_dio_logger: ^1.3.1
    provider: ^6.0.5
    shared_preferences: ^2.2.0
    syncfusion_flutter_charts: ^22.2.7
    ionicons: ^0.2.2
    url_strategy: ^0.2.0

dev_dependencies:
    flutter_test:
        sdk: flutter
    flutter_lints: ^2.0.0

flutter:
    uses-material-design: true

    assets:
        - .env
